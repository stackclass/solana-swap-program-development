In this stage, you'll implement time-based offer expiration.

### Step 1: Implement solution

Add expiration time to the Offer account and validate expiration in take_offer.

### Step 2: Run tests

```sh
git add .
git commit --allow-empty -m "[any message]"
git push origin main
```
