In this stage, you'll implement partial fill support.

### Step 1: Implement solution

Add filled amount tracking and update take_offer to support partial fills.

### Step 2: Run tests

```sh
git add .
git commit --allow-empty -m "[any message]"
git push origin main
```
